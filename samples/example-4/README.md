### Example-3

Small project, but has lots of playbooks so it's in a separate folder

```
.
├── README.md
├── ansible.cfg
├── hosts
└── playbooks
    ├── bianca-blog.yml
    ├── provision.yml
    └── site.yml
```